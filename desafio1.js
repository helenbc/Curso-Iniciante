const makes = ["primer","base","corretivo"];

console.log(makes);

makes.push("contorno");

console.log(makes);

makes.splice(2,4);

/*makes.shift();

console.log(makes);

makes.pop();
*/
console.log(makes);

